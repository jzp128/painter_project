export default class Drawable {
    constructor(startx, starty, strokeColor, fillColor, type) {
      this.startx = startx;
      this.starty = starty;
      this.type = type;
      this.strokeColor = strokeColor;
      this.fillColor = fillColor;
    }
}